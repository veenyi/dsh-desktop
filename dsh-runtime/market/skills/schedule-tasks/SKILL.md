---
name: schedule-tasks
description: 用自然语言描述周期任务，编排 /schedule 自动执行
---

# 定时任务编排

用自然语言描述周期任务，编排 /schedule 自动执行。

## 要点
1. 明确任务内容、执行周期（cron 或自然语言）与首次执行时间
2. 用 /schedule 命令创建/查看任务（对话输入 / 查看命令列表）
3. 任务描述要自包含：做什么、输出到哪、失败怎么办
4. 结合 IM 渠道：可让任务完成后推送到钉钉/飞书/企微群
5. 定期检查任务执行历史，清理失效任务
